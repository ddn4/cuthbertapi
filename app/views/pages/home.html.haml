%h1 Cuthbert API
%p
  %b juxtapose api url:
  = Configuration.juxtapose_api_url
  
%h3 Home#index
%p Find me in app/views/home/index.html.haml
-# - @user.each do |user|
  -# %p
    -# User:
    -# = link_to user.name, user
