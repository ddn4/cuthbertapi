%h1 Listing users

%table
  %tr
    %th Login
    %th ID
    %th Juxtapose API Id
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.login
      %td= user.id
      %td= user.juxtapose_id
      %td= link_to 'Show', user
      %td= link_to 'Edit', edit_user_path(user)
      %td= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New User', new_user_path
